<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="app.yummyos.board.notice.dao.CommDao">
	
	<insert id="insertComm" parameterType="app.yummyos.board.notice.dto.CommDto">
		insert into comm values(c_seq.nextval, #{id}, #{content}, sysdate, #{no})
	</insert>
	<delete id="deleteComm" parameterType="int">
		delete from comm where cno = #{cno}
	</delete>
	<select id="selectComm" parameterType="int" 
	resultType="app.yummyos.board.notice.dto.CommDto">
		select * from comm where no = #{no} order by regdate desc
	</select>
</mapper>
